{{title 'Review Character'}}
<h1>Review {{model.name}}</h1>


<h2>View Details</h2>

<p>You can view the character's background, sheet, and RP hooks on their character page.  This will open in a new tab so you can easily flip back to this one when finished. You may also want to refer to the <a href="/wiki/character_creation">CG Guide</a>.</p>

       <p>
          {{#link-to 'char' model.id class="btn btn-primary" target="_blank"}}View Character{{/link-to}}
      </p>



<h2>View Errors</h2>

<p>This panel shows any error messages from the character's automated app review.</p>

       <p><span class="ansi-bright-red-fg">Red</span> issues should probably hold up the app.</p>
       <p><span class="ansi-bright-yellow-fg">Yellow</span> may be OK if it suits the character.</p>
       <hr>

<div class="panel panel-default">
    <div class="panel-body">
        <pre class="ansi app-status">
{{#if model.abilities}}
{{{ansi-format text=model.abilities}}}
{{/if}}
{{{ansi-format text=model.demographics}}}
{{{ansi-format text=model.background}}}
{{{ansi-format text=model.desc}}}
{{{ansi-format text=model.hooks}}}
{{#if model.ranks}}
{{{ansi-format text=model.ranks}}}
{{/if}}
{{#if model.custom}}
{{{ansi-format text=model.custom}}}
{{/if}}
        </pre>
    </div>
</div>

<div class="panel panel-default">

    <div class="panel-body">

       <p>Approve the character or send the app back for revisions using the button below.  Any notes you add in the textbox will be sent to the applicant via the approval job.</p>

       {{textarea value=appNotes cols="80" rows=5}}


      <p>
          {{#link-to 'job' model.job class="btn btn-default"}}Back to Job{{/link-to}}
          <button name="reject" {{action 'rejectChar'}} class="btn btn-warning">Send Back</button>
          <button name="approve" {{action 'approveChar'}} class="btn btn-success">Approve</button>
      </p>

    </div>
</div>

{{outlet}}

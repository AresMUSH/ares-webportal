<h1>Scenes</h1>

<p>The scenes archive catalogs roleplay scenes from the game.  Only completed scenes show up here.  You can filter by type of scene or </p>


{{#if isApproved}}
<div class="row">
    
    <div class="col pull-right">
        {{#link-to 'scenes.create' class="btn btn-primary"}}Create Scene{{/link-to}}
    </div>
    
</div>
{{/if}}

<p><b>Select Scenes:</b> 

{{#power-select selected=filter options=sceneFilters onchange=(action "filterChanged") search=(action "searchScenes")as |name|}}
  {{name}}
{{/power-select}}
</p>

{{scene-list scenes=pageScenes}}

{{#each pagesList as |p|}}
{{#if (eq p page)}}
<button class="btn btn-primary" {{action "gotoPage" p}}>{{p}}</button>
{{else}}
<button class="btn btn-default" {{action "gotoPage" p}}>{{p}}</button>
{{/if}}
{{/each}}

{{outlet}}
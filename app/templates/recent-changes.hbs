{{title 'Recent Changes'}}
<h1>Recent Changes</h1>

<p>This list includes wiki pages and profiles.  For scenes, check the {{#link-to 'scenes'}}Scenes{{/link-to}} archive.</p>

<table class="table table-striped">
    
   {{#each model as |change|}}
   <tr>
       <td>
         {{#if (eq change.type 'char')}}
         Character
         {{else if (eq change.type 'wiki')}}
         Wiki Page
         {{else if (eq change.type 'event')}}
         Event
         {{else if (eq change.type 'scene')}}
         Scene
         {{else if (eq change.type 'file')}}
         File
         {{else}}
         {{change.type}}
         {{/if}}
       </td>
       <td>
           {{change.timestamp}}
       </td>
       <td>
           {{recent-change-link change=change}}
       </td>
       <td>
           {{change.author}}
       </td>
       <td>
         {{#if (eq change.type 'char')}}
           {{#link-to 'char-source' change.data.char_name change.data.version_id}}(history){{/link-to}}
         {{else if (eq change.type 'wiki')}}
           {{#link-to 'wiki-source' change.data.page_name change.data.version_id}}(history){{/link-to}}
         {{/if}}
       </td>
   </tr>
   {{/each}}

</table>

{{outlet}}
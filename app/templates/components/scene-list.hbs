<table class="table table-striped">


<thead>
    <tr>
        <th>Scene</th>
        <th>Participants</th>
    </tr>
</thead>

<tbody>

{{#each scenes as |scene|}}
    <tr>
        <td>
        <a href="/scene/{{scene.id}}">{{scene.icdate}} - {{scene.title}}</a>
        
        {{scene-tag scene=scene}}
       
        <p>{{scene.summary}}</p>
        </td>
        <td>
            {{#each scene.participants as |p|}}
            <a href="/char/{{p.name}}">{{p.name}}</a>
            {{/each}}
        </td>
    </tr>
{{/each}}

</tbody>
</table>
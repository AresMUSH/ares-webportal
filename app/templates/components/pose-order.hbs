<div class="scene-pose-order">
<label>Pose Order:</label> {{#each poseOrder as |p|}}
{{#if (eq p.name playerName)}}
  <span class="your-pose-order">{{p.name}}</span>
{{else}}
  {{p.name}}
{{/if}}
<span class="hint">{{p.timeString}}</span>
{{/each}}
</div> 

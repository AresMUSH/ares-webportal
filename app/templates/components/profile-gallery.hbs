<div class="panel panel-info">
{{#if char.profile_gallery}}
    {{#each char.profile_gallery as |g|}}
        {{#link-to 'file' char.profile_image}}
        <img src="/uploads/{{g}}" class="profile-gallery">
        {{/link-to}}
    {{/each}}
{{/if}}
<div class="clearfix"></div>
</div>

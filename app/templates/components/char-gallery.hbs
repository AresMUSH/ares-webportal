       <div>

          <h3>{{title}}</h3>
         
          {{#each chars as |c|}}
    
          <div class="profile-gallery">
              {{char-icon char=c}}
          </div>
          {{/each}}
      
       </div>

     <div class="clearfix"> </div>

window.deprecationWorkflow = window.deprecationWorkflow || {};
window.deprecationWorkflow.config = {
  workflow: [
    
    /* 
    { handler: "silence", matchId: "ember-global" },
    
    Actions are silence, log, throw
    */
  ]
};